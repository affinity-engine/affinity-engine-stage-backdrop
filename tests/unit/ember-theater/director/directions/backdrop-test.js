import Backdrop from 'ember-theater-director-backdrop/ember-theater/director/directions/backdrop';
import { module, test } from 'qunit';

module('Unit | Ember Theater Direction | backdrop');

// Replace this with your real tests.
test('it works', function(assert) {
  var backdrop = Backdrop.create();
  assert.ok(backdrop);
});
